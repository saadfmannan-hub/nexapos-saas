[tool.ruff]
line-length = 100
target-version = "py312"
exclude = ["*/migrations/*", ".venv", "staticfiles"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "DJ"]
ignore = ["E501"]

[tool.black]
line-length = 100
extend-exclude = "migrations|.venv|staticfiles"
